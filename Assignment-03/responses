The relationship between an IP address and and the DNS is that the domain name server acts as a link to the IP address. The DNS is also the text version of the IP address.

A cookie is a text file that contains small pieces of data.

The protocol, domain, and path make up the essential parts of a URL.
